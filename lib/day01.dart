void main() {
  String age = 'hello';
  String index = '01234';
  String result;

  result = age + index; // this is how we concatenate strings
// this is how we get the length of a string
  print(age.length);
  print(index.length);
  print(age[0]); // this is how we get the first character of a string
  print(result);
}
